# PayPal API Style Guide Example

This example focuses on predictable write operations.

- `captures.http` demonstrates idempotency keys, correlation IDs, and stable money fields.
- `conflict.json` shows how a duplicate idempotency key returns a machine-readable conflict response.
