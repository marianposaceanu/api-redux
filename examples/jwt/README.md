# JSON Web Token Example

This Sinatra example renders a small UI for experimenting with a JWT-backed API call flow.

- `app.rb` serves the demo page.
- `token-payload.json` shows the claims a client might decode before calling an API.
