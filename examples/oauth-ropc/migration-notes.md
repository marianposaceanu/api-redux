# Migration Notes

- ROPC requires the client to collect the user's password directly.
- Prefer authorization code with PKCE for browser and mobile clients.
- Keep this flow only for tightly controlled first-party legacy clients during migration.
